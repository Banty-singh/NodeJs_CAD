# NodeJs_CAD
Overview This project is designed to manage relationships between Chartered Accountants (CAs) and their clients.  A CA can handle multiple clients.  Each client is associated with only one CA.  CAs can generate and send invoices to their clients.  Clients can view their invoices anytime through the system. 
